实现流程:
    1,将tensorflow从单机式改为分布式形式,分清训练程序部分和测试程序部分;
    2,改写程序保存训练之后的模型,保存的路径需要和yaml配置文件中声明的相对应,上传打包之后的程序文件至github,得到url在执行容器命令时使用;
    3,类比于手写数字识别程序的流程训练模型,只是执行的命令不再单纯是curl下载一个文件而是下载之前上传的文件包,之后解压,执行里面的用于训练的py程
序,最终得到位于参数服务器上的训练好的模型文件.分两阶段训练,分别得到图片识别和诗词生成的模型文件;
    4,将两个模型文件打包上传到github,用于之后的测试使用;
    5,测试的时候执行的是测试程序,利用网页服务来获取图片和展示诗词,加载两个模型文件,测试得到sentence,传回网页前端展示.